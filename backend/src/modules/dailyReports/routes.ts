import { FastifyPluginAsync } from 'fastify';
const calc=(s:string,e?:string)=>e?Math.max(0,Math.floor((+new Date(e)-+new Date(s))/60000)):0;
export const dailyReportsRoutes: FastifyPluginAsync = async (app)=>{ app.get('/daily-reports',async(req:any)=>app.prisma.dailyReport.findMany({where:{teamId:req.query.teamId,userId:req.query.userId||undefined}})); app.get('/daily-reports/me',async(req:any)=>app.prisma.dailyReport.findFirst({where:{teamId:req.query.teamId,workDate:new Date(req.query.workDate)}})); app.post('/daily-reports',async(req:any)=>app.prisma.dailyReport.create({data:req.body})); app.get('/daily-reports/:dailyReportId',async(req:any)=>app.prisma.dailyReport.findUnique({where:{dailyReportId:req.params.dailyReportId},include:{entries:{include:{task:{include:{project:true}}}}}})); app.patch('/daily-reports/:dailyReportId',async(req:any)=>app.prisma.dailyReport.update({where:{dailyReportId:req.params.dailyReportId},data:req.body})); app.delete('/daily-reports/:dailyReportId',async(req:any)=>app.prisma.dailyReport.delete({where:{dailyReportId:req.params.dailyReportId}})); app.post('/daily-reports/:dailyReportId/entries',async(req:any)=>app.prisma.dailyReportEntry.create({data:{...req.body,dailyReportId:req.params.dailyReportId,minutes:calc(req.body.startTime,req.body.endTime)}})); app.patch('/daily-report-entries/:entryId',async(req:any)=>app.prisma.dailyReportEntry.update({where:{entryId:req.params.entryId},data:{...req.body,minutes:calc(req.body.startTime,req.body.endTime)}})); app.delete('/daily-report-entries/:entryId',async(req:any)=>app.prisma.dailyReportEntry.delete({where:{entryId:req.params.entryId}})); };
